buildscript {

    repositories {
        jcenter()
        mavenCentral()
        mavenLocal()
        maven {
            url "https://plugins.gradle.org/m2/"
        }
        maven {
            url 'http://10.10.14.35:8081/repository/maven-releases/'
        }
    }

    dependencies {
        classpath "org.springframework.boot:spring-boot-gradle-plugin:${springBootVersion}"
        classpath "io.spring.gradle:dependency-management-plugin:${depManVersion}"
        classpath "io.freefair.gradle:lombok-plugin:${lombokVersion}"
    }
}

subprojects {

    buildscript {
        repositories {
            mavenCentral()
            mavenLocal();
            maven {
                url "https://plugins.gradle.org/m2/"
            }
            maven {
                url "http://10.10.14.35:8081/repository/maven-releases/"
                credentials {
                    username 'admin'
                    password '2802nK.'
                }
            }
        }
    }

    repositories {
        mavenCentral()
        jcenter()
        mavenLocal();
        maven {
            url 'http://10.10.14.35:8081/repository/maven-releases/'
            credentials {
                username 'admin'
                password '2802nK.'
            }
        }
    }

    apply plugin: 'idea'
    apply plugin: 'java'

    apply plugin: 'maven-publish'
    apply plugin: 'maven'
    apply plugin: 'wrapper'

    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    apply plugin: "io.freefair.lombok"

    tasks.withType(Javadoc) {
        failOnError false
        options.addStringOption('Xdoclint:none', '-quiet')
        options.addStringOption('encoding', 'UTF-8')
        options.addStringOption('charSet', 'UTF-8')
    }

    publishing {
        repositories {
            maven {
                name = "GitHubPackages"
                url = uri("https://maven.pkg.github.com/kutay-celebi")
                credentials {
                    username = "kutay-celebi"
                    password = "a4d378bef3b053840933f05004a10776cb9a3538"
                }
            }
        }
        publications {
            gpr(MavenPublication) {
                from(components.java)
            }
        }
    }
}

